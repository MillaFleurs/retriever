interface:
  display_name: "Retriever Onboard"
  short_description: "Build a scheduled local job-search profile."
  default_prompt: "Use $retriever-onboard to build my job-search profile and set a daily, weekly, or monthly cadence."
