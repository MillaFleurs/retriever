interface:
  display_name: "Retriever Uninstall"
  short_description: "Safely remove schedules or reset a test profile."
  default_prompt: "Use $retriever-uninstall to reset Retriever schedules and local state for a clean test."
