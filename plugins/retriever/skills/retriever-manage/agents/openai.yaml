interface:
  display_name: "Retriever Manage"
  short_description: "Update targets, cadence, and archives."
  default_prompt: "Use $retriever-manage to update my Retriever targets, schedule, or archives."
