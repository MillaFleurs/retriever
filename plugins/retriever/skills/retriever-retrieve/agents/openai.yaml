interface:
  display_name: "Retriever Retrieve"
  short_description: "Find new jobs and manage scheduled checks."
  default_prompt: "Use $retriever-retrieve to check my active company sites or update my scheduled job search."
